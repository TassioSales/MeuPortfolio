services:
  enem_insights:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      - ENEM_DATA_PATH=${ENEM_DATA_PATH:-}
      - ENEM_SAMPLE_SIZE=${ENEM_SAMPLE_SIZE:-100000}
    restart: unless-stopped
