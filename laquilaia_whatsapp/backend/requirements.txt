# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
# Driver async exigido por create_async_engine — sem ele o app não inicia.
asyncpg==0.29.0
alembic==1.12.1
psycopg2-binary==2.9.9
prisma==0.13.0

# Data Validation
pydantic==2.5.0
# Exigido pelo tipo EmailStr usado nos schemas.
email-validator==2.1.1
pydantic-settings==2.1.0

# Authentication & Security
pyjwt==2.8.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 não lê a versão do bcrypt >= 4.1 e cai num backend de
# fallback, cuspindo traceback a cada hash. 4.0.1 é a última compatível.
bcrypt==4.0.1

# AI/LLM
anthropic==0.7.0

# Cache & Message Queue
redis==5.0.0
aioredis==2.0.1

# HTTP Client
httpx==0.25.0
requests==2.31.0

# Logging
loguru==0.7.2

# Async & Scheduling
asyncio==3.4.3
aiofiles==23.2.1
apscheduler==3.10.4

# Environment
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# CORS
fastapi-cors==0.0.6
