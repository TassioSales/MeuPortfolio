# Configurações de Exemplo para o Finance Project
# Renomeie este arquivo para .env ou configure as variáveis de ambiente no seu sistema

# --- Geral ---
# Define se o modo de debug está ativado (True para desenvolvimento, False para produção)
django_debug=True

# Chave secreta do Django (Gere uma nova para produção!)
SECRET_KEY=sua_chave_secreta_aqui

# --- Banco de Dados (PostgreSQL) ---
# Se não preenchido, o sistema usará SQLite automaticamente (db.sqlite3)
# DB_NAME=finance_db
# DB_USER=postgres
# DB_PASSWORD=sua_senha
# DB_HOST=localhost
# DB_PORT=5432

# --- Banco de Dados (SQLite Personalizado) ---
# Caminho opcional para o arquivo SQLite
# SQLITE_DB_PATH=C:/Caminho/Para/Seu/db.sqlite3

# --- Outros ---
# ALLOWED_HOSTS=localhost,127.0.0.1
